<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.childrenofthecorn.pockerplanner"
          xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name="com.childrenofthecorn.pockerplanner.App"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name="com.childrenofthecorn.pockerplanner.presentation.ui.activities.JointToSprintActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name="com.childrenofthecorn.pockerplanner.presentation.ui.activities.SprintInfoActivity"/>
    </application>

</manifest>